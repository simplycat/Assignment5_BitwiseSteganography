# Assignment 5: Bitwise Steganography
A C program that uses simple steganography to encode a secret message inside an image.

## Run the Program to Decode the Secret Message
```
./steg encode input.ppm secret.bin output.ppm
./steg decode output.ppm recovered.bin
cat recovered.bin
```
